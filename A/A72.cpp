#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); ++i)
using ll = long long;

int main(){
    int x, t;
    cin >> x >> t;
    if(x-t >= 0) cout << x-t << endl;
    else cout << 0 << endl;
    return 0;
}