#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for (int i = 0; i < (n); ++i)
using ll = long long;

int main(){
    int a, p;
    cin >> a >> p;
    int sum = a*3+p;
    cout << sum/2 << endl;
    return 0;
}